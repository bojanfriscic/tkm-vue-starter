import * as auth from './modules/auth';
import * as registry from "./modules/registry";

// const violations = () => [
//     { id: 1, name: 'test' },
//     { id: 2, name: 'test2' },
// ];

export default {
    auth,
    registry,
    // violations,
};
